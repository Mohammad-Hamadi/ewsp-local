apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: backend-ingress-from-dashboard
  namespace: ewsp
  labels:
    app.kubernetes.io/part-of: ewsp
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: backend
      app.kubernetes.io/part-of: ewsp
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ewsp
          podSelector:
            matchLabels:
              app.kubernetes.io/name: dashboard
              app.kubernetes.io/part-of: ewsp
      ports:
        - protocol: TCP
          port: 8080
